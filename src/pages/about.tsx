import { Meta } from '@/layout/Meta';
import { Main } from '@/templates/Main';

const About = () => (
  <Main
    meta={
      <Meta
        title="About the gallery"
        description="About page of the gallery and the artist"
      />
    }
  >
    <div></div>
  </Main>
);
export default About;
